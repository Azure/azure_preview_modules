- name: Prepare random number
  set_fact:
    rpfx: "{{ resource_group | hash('md5') | truncate(7, True, '') }}{{ 1000 | random }}"
  run_once: yes

- name: Gather facts Firewall Rule
  azure_rm_mariadbfirewallrule_facts:
    resource_group: "{{ resource_group }}"
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
      - output.firewall_rules.xxxunknownxxx.id != None
      - output.firewall_rules.xxxunknownxxx.name != None

- name: Gather facts Firewall Rule
  azure_rm_mariadbfirewallrule_facts:
    resource_group: "{{ resource_group }}"
  register: output
- name: Assert that facts are returned
  assert:
    that:
      - output.changed == False
      - output.firewall_rules.xxxunknownxxx.id != None
      - output.firewall_rules.xxxunknownxxx.name != None
      - output.firewall_rules.xxxunknownxxxsecond.id != None
      - output.firewall_rules.xxxunknownxxxsecond.name != None
