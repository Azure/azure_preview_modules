- name: Prepare random number
  set_fact:
    rpfx: "{{ resource_group | hash('md5') | truncate(7, True, '') }}{{ 1000 | random }}"
  run_once: yes

- name: Create instance of Server -- check mode
  azure_rm_mariadbserver:
    resource_group: "{{ resource_group }}"
    name: "mdbs{{ rpfx }}"
    sku:
      name: GP_Gen5_2
      tier: GeneralPurpose
      capacity: 2
      family: Gen5
    location: westus
    storage_mb: 51200
    ssl_enforcement: enabled 
    administrator_login: zimxyz
    administrator_login_password: Testpasswordxyz12!
  check_mode: yes
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create instance of Server
  azure_rm_mariadbserver:
    resource_group: "{{ resource_group }}"
    name: "mdbs{{ rpfx }}"
    sku:
      name: GP_Gen5_2
      tier: GeneralPurpose
      capacity: 2
      family: Gen5
    location: westus
    storage_mb: 51200
    ssl_enforcement: enabled 
    administrator_login: zimxyz
    administrator_login_password: Testpasswordxyz12!
  register: output
- name: Assert the resource instance is well created
  assert:
    that:
      - output.changed

- name: Create again instance of Server
  azure_rm_mariadbserver:
    resource_group: "{{ resource_group }}"
    name: "mdbs{{ rpfx }}"
    sku:
      name: GP_Gen5_2
      tier: GeneralPurpose
      capacity: 2
      family: Gen5
    location: westus
    storage_mb: 51200
    ssl_enforcement: enabled 
    administrator_login: zimxyz
    administrator_login_password: Testpasswordxyz12!
  register: output
- name: Assert the state has not changed
  assert:
    that:
      - output.changed == false

- name: Delete instance of Server -- check mode
  azure_rm_mariadbserver:
    resource_group: "{{ resource_group }}"
    name: "mdbs{{ rpfx }}"
    state: absent
  check_mode: yes
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete instance of Server
  azure_rm_mariadbserver:
    resource_group: "{{ resource_group }}"
    name: "mdbs{{ rpfx }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed

- name: Delete unexisting instance of Server
  azure_rm_mariadbserver:
    resource_group: "{{ resource_group }}"
    name: "mdbs{{ rpfx }}"
    state: absent
  register: output
- name: Assert the state has changed
  assert:
    that:
      - output.changed == false
